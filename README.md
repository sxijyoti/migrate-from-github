# migrate-from-github

script cuz why not (for ppl who value time and wanna automate life)

# migration script for codeberg

## TO RUN

```bash
chmod +x migration-to-codeberg.sh
./migration-to-codeberg.sh
```
Make sure to update values for `GITHUB USERNAME` `GITHUB_TOKEN` `CODEBERG_USERNAME` `CODEBERG_TOKEN` and the array values for `OWNERS` and `REPOSITORIES`
